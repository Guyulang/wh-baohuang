# 部署到 Render（免费云平台）

这个游戏需要一个常驻的 Node.js 服务器（WebSocket 联机），Render 免费版支持，适合你们朋友一起玩。

## 一、准备工作（各注册一次，都免费）

1. **GitHub 账号**：https://github.com —— 用来存放代码（没有就注册）
2. **Render 账号**：https://render.com —— 用 GitHub 账号一键登录即可

## 二、把代码推到 GitHub

1. 在 GitHub 新建一个仓库（名字随意，如 `baohuang`），**不要**勾选自动生成 README。
2. 在本地项目目录打开终端，依次执行：

```bash
git init
git add .
git commit -m "保皇游戏 v1.0"
git branch -M main
git remote add origin https://github.com/你的用户名/baohuang.git
git push -u origin main
```

## 三、在 Render 部署

### 方式 A：Blueprint 一键部署（推荐）

1. 打开 https://render.com → 右上角 **New** → **Blueprint**
2. 授权并选择你的 `baohuang` 仓库
3. Render 会自动读取 `render.yaml`，识别出 `baohuang` 服务
4. 点 **Apply**，等待部署完成（首次约 2-5 分钟）

### 方式 B：手动创建 Web Service

1. Render → **New** → **Web Service** → 连接你的 GitHub 仓库
2. 按下面填写：

| 配置项 | 值 |
|---|---|
| Name | baohuang（随意） |
| Runtime | Node |
| Build Command | `npm install` |
| Start Command | `npm start` |
| Instance Type | Free |

3. 点 **Create Web Service**，等待部署完成

## 四、开始玩

1. 部署完成后，Render 会给你一个链接，形如 `https://baohuang-xxxx.onrender.com`
2. 你（房主）用手机打开这个链接 → 输入昵称 → **创建房间**
3. 把房间码（或二维码）发给另外 4 个朋友
4. 朋友打开链接 → 输入昵称 → **加入** → 输入房间码
5. 5 人齐 → 房主点 **开始游戏**

## 常见问题

- **首次打开很慢**：Render 免费版在无访问时（15 分钟）会休眠，唤醒约需 30-50 秒。朋友都在线玩时不会休眠。
- **改代码后更新**：`git push` 到 main 分支，Render 会自动重新部署（`autoDeploy: true`）。
- **健康检查**：Render 会访问 `/health`，返回 `ok` 即表示服务正常。

## 本地测试（不上公网时）

```bash
node server.js
```
然后浏览器打开 http://localhost:8080（手机和电脑连同一 WiFi 时，手机可访问 `http://电脑IP:8080`）。
